package CrackingTheCodingInterview.Udemy;

public class ReverseWordsInString {
    public static void main(String[] args) {
        
    }

    //keep the order of the words the same, but reverse the letters within each word
    public static void reverseLettersInWord(String str) {
        //regular expression for removing extra whitespace
        S
        for (int i = 0; i < arr.length; i++) {
            String curr = arr[i];
            int i = 0;
            int j = curr.length-1;
            while (i < j) {
                String tmp =
            }
        }
    }
}